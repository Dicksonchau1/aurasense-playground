export interface SweepPromotedEvent {
  kind: "sweep_promoted";
  sweep_id: string;
  drone_id: string;
  building_id: string;
  defects_found: number;
  promoted_by: string;
  ts: string;
}export interface SweepPromotedEvent {
  kind: "sweep_promoted";
  sweep_id: string;
  drone_id: string;
  building_id: string;
  defects_found: number;
  promoted_by: string;
  ts: string;
}
